import styles from './styles'

const ACTION_PERFORMED = {
  STARTED: 'started',
  STOPPED: 'stopped',
  DELETED: 'deleted',
  UPDATED: 'updated',
  CREATED: 'added',
};

export  default ACTION_PERFORMED;
export const styles;